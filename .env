# Frontend (Vite)
# URL base de tu backend
VITE_API_URL=http://localhost:3000

# Endpoint de creación de reservas.
# Puede ser ruta relativa (se concatena con VITE_API_URL) o URL completa.
VITE_BOOKING_ENDPOINT=/api/sessions/bookings